<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quan.appaddressingall.dao.APIInstanceDao">
    <select id="selectAllInstance" resultType="com.quan.appaddressingall.entity.APIInstance">
        select i.*, concat(a.app_key, "&amp;", i.interface_name) as key_name
        from app_id_meta as a
                 join interface_instance as i on a.app_id = i.app_id;
    </select>

    <select id="selectInstanceByAppId" resultType="com.quan.appaddressingall.entity.APIInstance">
        select i.* ,a.app_secret,concat(a.app_key,"&amp;",i.interface_name,"&amp;",i.version) as key_name_version from
        app_id_meta as a join interface_instance
        as i on a.app_id = i.app_id where
        <foreach collection="list" item="appId" separator="or">
            i.app_id = #{appId}
        </foreach>

    </select>

    <insert id="addInstance">
        insert into interface_instance (interface_name, address, port, app_id, priority)
        values
        <foreach collection="list" item="instance" separator=",">
            (#{instance.interfaceName},#{instance.address},#{instance.port},#{instance.appId},#{instance.isDefault},#{instance.priority})
        </foreach>

    </insert>
</mapper>